<UserControl x:Class="nvGPUMonitor.Controls.DonutGauge"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             MinWidth="192" MinHeight="192">
  <Grid>
    <Path x:Name="ArcBg" StrokeThickness="24" Stroke="#E5E5EA" Fill="Transparent" />
    <Path x:Name="ArcPath" StrokeThickness="24" StrokeEndLineCap="Round" Stroke="#3A7BFF" Fill="Transparent" />
    <Grid x:Name="Overlay" VerticalAlignment="Center" HorizontalAlignment="Center">
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>
      </Grid.RowDefinitions>
      <TextBlock x:Name="CaptionText" Grid.Row="0" Text="LABEL" FontSize="28" FontWeight="Bold"
                 HorizontalAlignment="Stretch" TextAlignment="Center"/>
      <TextBlock x:Name="ValueText" Grid.Row="1" Text="0%" FontSize="{Binding ElementName=CaptionText, Path=FontSize}"
                 FontWeight="Bold" HorizontalAlignment="Stretch" TextAlignment="Center" Margin="0,2,0,0"/>
      <TextBlock x:Name="DetailText" Grid.Row="2" Text="" FontSize="13" Foreground="#666"
                 HorizontalAlignment="Stretch" TextAlignment="Center" Margin="0,2,0,0"/>
    </Grid>
  </Grid>
</UserControl>